# Build output
*.exe

# Generated Windows resource object (regenerated by build.sh / rsrc)
*.syso

# Per-user runtime config (created next to the executable at runtime)
config.json

# Debug marker / logs
debug.on
*.log
